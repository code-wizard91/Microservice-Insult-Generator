- hosts: test
  become: true
  vars:
    service: nginx
  tasks:
  - name: Install "{{ service }}"
    apt:
      name: "{{ service }}"
      state: latest
      update_cache: true
  - name: Start "{{ service }}" Service
    service:
      name: "{{ service }}"
      state: started

